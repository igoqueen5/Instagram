<%= form_for(@pict) do |f| %>
  <%= f.label :comment %>
  <%= f.text_field :comment, class: 'form-control' %>
  <br>
  <%= f.submit'投稿',class: 'btn btn-primary btn-block' %>
<% end %>

<%= link_to "フォト一覧画面にもどる", picts_path %>
